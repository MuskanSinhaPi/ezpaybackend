# Application name
spring.application.name=ezpaybackend

# Oracle datasource configuration
spring.datasource.url=jdbc:oracle:thin:@//localhost:1521/XE
spring.datasource.username=system
spring.datasource.password=ezpay123
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver

# JPA / Hibernate configuration
spring.jpa.hibernate.ddl-auto=update   # auto-create/update tables; use 'validate' in production
spring.jpa.show-sql=true               # prints SQL queries in console
spring.jpa.properties.hibernate.format_sql=true  # makes SQL readable

# Server configuration
server.port=9090
